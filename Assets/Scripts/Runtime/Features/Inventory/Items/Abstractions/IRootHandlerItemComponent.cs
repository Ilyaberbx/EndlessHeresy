using EndlessHeresy.Runtime.Inventory.Items.Implementations;

namespace EndlessHeresy.Runtime.Inventory.Items.Abstractions
{
    public interface IRootHandlerItemComponent
    {
        void Initialize(ItemRoot root);
    }
}